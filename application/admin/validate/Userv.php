<?php

namespace app\admin\validate;

use think\Validate;

class Userv extends Validate
{
    /**
     * 定义验证规则
     * 格式：'字段名'	=>	['规则1','规则2'...]
     *
     * @var array
     */	
	protected $rule = [
        'name|用户名'=>'require|min:2|max:10',
        'password|密码'=>'require|min:6|max:16'
    ];
    
    /**
     * 定义错误信息
     * 格式：'字段名.规则名'	=>	'错误信息'
     *
     * @var array
     */	
    protected $message = [];
}
